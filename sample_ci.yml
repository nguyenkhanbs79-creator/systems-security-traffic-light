name: Sample CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: echo "API_KEY=1234567890SECRETKEY123456"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy script
        run: curl http://example.com/install.sh | bash
